Var integer cursor-x
Var integer cursor-y

Sys Fs read
'\n' String split

: f
	#dup 0 #swap Sys Terminal set-cursor
	@dup @refs Window print
	;

0 if #dup @dup @refcount 20 #min < then
	#dup f
	1 +
repeat @drop #drop

Sys Terminal wait
