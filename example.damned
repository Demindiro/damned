Var integer cursor-x
Var integer cursor-y

Sys Fs read
'\n' String split

: f
	#dup 0 #swap Sys Terminal set-cursor
	@dup @refs Window print
	;

: #drop2 #swap #drop ;
: #dup2 #2dup #drop ;

Window size #drop2
@dup @refcount #min
1 -

0 if #2dup > then
	#dup f
	1 +
repeat @drop #drop

Sys Terminal wait
